# Blocklist

A small custom ad-block filter list.

## Contents

`blocklist.txt` contains rules in Adblock Plus filter syntax. Add the raw file
URL to a compatible content blocker as a custom filter list.

## Scope

The current list blocks requests to `ads.zorvec.name` and its subdomains.
